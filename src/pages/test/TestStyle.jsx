import styled from "styled-components";

export const TestWrapper = styled.form`
    width: 700px;
    margin: 50px auto;
    
    h1, p {
        font-weight: 500;
    }

    .categories {
        padding: 0 20px;
    }

    .categories p {
        color: #878787;
        font-weight: 400;
        font-size: 14px;
        cursor: pointer;
    }
`